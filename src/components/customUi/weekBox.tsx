import { DayBox } from '@/components/customUi/index';

const WeekBox = () => {
  return (
    <div className="flex flex-1 h-full">
      <DayBox></DayBox>
      <DayBox></DayBox>
      <DayBox></DayBox>
      <DayBox></DayBox>
      <DayBox></DayBox>
      <DayBox></DayBox>
      <DayBox></DayBox>
    </div>
  );
};

export default WeekBox;
